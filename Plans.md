Это планы которые нужно реализовать - 

=== СТАТУС (обновлено 2026-07-15) ===
Сверх исходного списка (1-25, весь закрыт ранее): полная интернационализация
приложения (ru/en/sk) — Данил показывает приложение в Словакии, плюс пригодится
на будущее. Коммиты 46caaff/a6a4dc0/0484f27/322dd31. Библиотека — next-intl.

Адреса с префиксом языка (/en/finance, /sk/finance) — выбор Данила, а не
рекомендованный мной более простой localStorage-тоггл (объяснял риски, он
предпочёл URL-адреса). Русский — дефолтный locale БЕЗ префикса
(`localePrefix: 'as-needed'`, i18n/routing.ts), поэтому /today, /finance и
т.д. остаются теми же адресами, что и раньше — важно, т.к. start_url уже
установленной на телефон PWA-иконки кэшируется ОС и не обновляется задним
числом (это уже ловили в этой же сессии на другой задаче). `localeDetection:
false` — иначе next-intl сам определял бы язык по Accept-Language браузера и
мог увести холодный запуск иконки на /en/today.

Переведено: BottomNav, сплэш, login/register/join (+ ошибки API), Сегодня,
Трекер (+ вся статистика/серии/расписания), Финансы (самый большой экран —
счета/операции/кредиты-долги/кабинеты/категории/бюджеты/курсы/отчёты),
Напоминания (+ сайдбар/поиск/шаринг разделов) и Dashboard (календарь/шторка
события с повторами). Плюрализация — через ICU `{count, plural, ...}`
(next-intl/Intl.PluralRules), заменили две самодельные функции `plural()`,
которые правильно считали только русский мод-10/мод-100. Даты/деньги —
через `Intl` с локалью вместо хардкода `'ru-RU'`. Сидинг дефолтных категорий
Финансов в новом кабинете — на языке того, кто первым открыл экран (была
такая договорённость, это следствие уже существующей ленивой схемы сидинга).
Переключатель языка — components/LanguageSwitcher.tsx, одна пилюля RU/EN/SK
в app/[locale]/(main)/layout.tsx, на Link с explicit locale из
@/i18n/navigation — сама остаётся на текущей странице под новым префиксом.

Сознательно НЕ тронуто (второй этап на будущее, договорились с Данилом):
голосовой ассистент (Gemini/VoiceAssistant) остаётся русскоязычным — system
prompt в app/api/assistant/route.ts и `recognition.lang = 'ru-RU'` не
трогали. Манифест (app/manifest.ts) — вне [locale]-дерева, ОС кэширует его
как и start_url, менять бессмысленно для уже установленной иконки.

Проверено: `npx tsc --noEmit` чисто на каждом коммите; Playwright-скриншоты
на всех 3 языках для каждого экрана (список/детали/шторки/настройки);
матрица редиректов (bare /today без 307 с валидным auth_token и без
NEXT_LOCALE — критично для PWA; /en/today, /sk/today напрямую; login/
register/join без петель редиректа во всех 3 локалях); переключатель языка
остаётся на странице (/finance → /en/finance → /sk/finance → /finance);
плюральные категории на граничных числах 1/2/5/11/21 сверены с
Intl.PluralRules для ru/en/sk (например ru: 21 → one, 5 и 11 → many — не
ошибка, встроенное поведение CLDR); сидинг категорий на языке визитёра
(NEXT_LOCALE=en → «Food/Groceries/Transport» вместо «Еда/Продукты»).
Тестовые аккаунты созданы и удалены по ходу проверки каждого экрана.

=== СТАТУС (обновлено 2026-07-14) ===
ВЕСЬ СПИСОК (1-25) ЗАКРЫТ. Пункты 1-12 — все 12 из 12, плюс №13, плюс пачка
доработок экрана «Сегодня» и Финансов сверх исходного списка (см. ниже),
плюс из второго списка (14-25) — №14/№16/№17/№18(частично)/№20/№22(частично)/
№23/№24 сделаны, №15/№19/№21/№25 не требовали работы (уже были готовы).
Единственное сознательно НЕ сделанное — настоящие push-уведомления (часть
№18/№22) и полная OAuth-синхронизация с календарём/импорт CSV (часть №17/№20) —
по итогам обсуждения решили, что не нужны/не стоят сложности.

№20 (экспорт в .ics) — DONE, коммит 93d3fb9. Кнопка «📅 Добавить в календарь
телефона» на задачах/событиях на Сегодня и в форме события в календаре
(Dashboard.tsx) — генерирует валидный iCalendar-файл на клиенте (весь день
или с временем, плюс RRULE если у события задано повторение). Проверено:
скачанный .ics и для события (со временем/повтором), и для задачи (весь
день) содержит корректные поля. lib/ics.ts.

№14 (повторяющиеся события) — DONE, коммит 3f15ca8. У события в календаре
(Dashboard.tsx, Главная) есть «Повторять: Нет/День/Неделя/Месяц/Год» —
день/число/месяц повторения берутся из даты самого события, отдельного
пикера дня недели и т.п. не делали (сознательно, проще и решает реальный
кейс — «день рождения 29 апреля каждый год»). Даты повторения считаются на
лету (lib/events.ts eventOccursOn) — новых строк в базе не создаётся,
сколько бы лет вперёд ни пролистать календарь (проверено — событие 2026 года
корректно всплыло в июле 2027). Правка/удаление — по всей серии сразу (это
одна строка), при редактировании повторяющегося события есть подсказка об
этом. Пока только для событий (Dashboard/Today), не для задач-напоминаний —
если понадобится повтор и для задач, там сложнее (нужна отметка выполнения
по каждому отдельному вхождению, а не общий чекбокс на всю серию).

№16 + №24 + №17 (фильтры/поиск/экспорт операций в Финансах) — DONE, коммит
a58f308. Над «Последние операции»: чипы категорий (только те, что реально
встречаются, «Все» сбрасывает), поиск по комментарию/категории/счёту/сумме,
переключатель сортировки по дате, кнопка «⬇ CSV» — экспортирует то, что
сейчас видно с учётом фильтра/поиска/сортировки (BOM для кириллицы в Excel).
Импорт сознательно не делали (см. договорённость ниже). Всё на клиенте, без
новых API.

№23 (шаблоны повторяющихся операций) — DONE. Максимум 5 штук на кабинет
(осознанно — чтобы не разрастаться в отдельный экран), никакого нового
экрана: чипсы прямо в форме «+ Новая операция» (только для расход/доход,
не для перевода/кредита). Тап по чипу подставляет счёт/сумму/категорию/
комментарий в уже открытую форму — сохраняет обычная кнопка «Записать»,
без авто-отправки (сумма иногда чуть отличается от раза к разу). Сохранить
текущее заполнение как шаблон — ссылка «☆ Сохранить как шаблон» под формой
(появляется только когда есть счёт+сумма и лимит 5 не исчерпан), удалить —
✕ прямо на чипе. Таблица finance_templates (миграция
scripts/migrate-op-templates.mjs), API /api/finance/templates (+[id]).
Проверено вживую: сохранение → переоткрытие формы (шаблон подтянулся) →
применение чипом → запись операции → удаление чипа — всё по отдельности
отработало верно.

Оплата кредита/долга из «+ Новая операция» — DONE, коммит 545e521. Раньше
единственный способ заплатить был ⚙ → Кредиты и долги → конкретный кредит —
неочевидно, спросил в чате «где платить рассрочку». Добавлена 4-я вкладка
«Кредит» в форме операции (выбор кредита из списка активных, сумма
предзаполняется, счёт списания). Заодно нашёл и починил смежный баг: платёж
по кредиту (ЛЮБЫМ способом — новая вкладка, кнопка в карточке кредита, быстрая
оплата на Сегодня) вообще не появлялся в «Последние операции», только в
истории платежей самого кредита — теперь пишется туда тоже (category
'credit:<id>', вне категорийных бюджетов), не редактируется через общую форму,
а удаление (с любой стороны — из истории кредита или из списка операций)
атомарно чистит обе стороны без «призраков». Миграция:
finance_credit_payments.txn_id.

№18/№22 (уведомления о платежах/долгах/превышении бюджета) — ЧАСТИЧНО DONE,
коммит c19e0db (прогноз «К концу месяца» из этого коммита позже УДАЛЁН, см.
ниже — 7be6c1a). Обсудили вслух область охвата: настоящие push-уведомления
(даже при закрытом приложении) — отдельная большая инфраструктура (VAPID-ключи,
подписки, серверный крон); решили пока НЕ делать, только «на входе» (человек
открыл — увидел). Осталось в силе из этого коммита:
- Превышение категорийного бюджета показывает сумму превышения (не только
  название категории) — отдельная карточка на Сегодня.
- Долги/рассрочки без графика платежей, но с близким/просроченным сроком
  погашения — тоже показываются в «Платежи» на Сегодня с кнопкой «Оплатить».
Остальное из второго списка ещё не начато (см. пункт «ВТОРОЙ СПИСОК» ниже
по итогам обсуждения 2026-07-12).

«Свободно на месяц» (ручной бюджет на Сегодня) — СДЕЛАНО и в тот же день
УБРАНО ОБРАТНО, коммит 7be6c1a. Обсудили реальную модель бюджета семьи: траты
делятся на (1) стабильные общие — уже покрыты категорийными бюджетами, и
(2) личные карманные у каждого свои суммы (напр. у жены 60 000 ₸/мес),
которые решили вести на словах между собой, а не в приложении — единое число
«Свободно на месяц» на кабинет не описывало ни то, ни другое. Убрано
полностью: карточка, API /api/finance/free-budget, lib/finance.ts
getFreeBudget/setFreeBudget, миграция. Если в будущем возвращаться к идее
личных карманных в приложении — считать per-человек (по автору операции
finance_txns.user_id), не per-кабинет.

ВТОРОЙ СПИСОК (14-25) — что уже выяснили при обсуждении 2026-07-12:
- №15 (синхронизация между устройствами) — ГОТОВО, ничего делать не требовалось:
  уже есть «из коробки» (данные в Neon, не в localStorage).
- №19 и №25 (голосовой/текстовый ввод одной фразой) — ГОТОВО: чат-ассистент
  (app/api/assistant/route.ts, переехал на Gemini — см. коммиты
  5222f21/c34fdea), выполняет несколько действий одной фразой.
- №21 (отчёты/аналитика по периодам) — ГОТОВО, ничего делать не требовалось:
  уже есть шторка «Тенденции и отчёты» в Финансах (расходы/доходы/деньги по
  месяцам, фильтр по категории).
- №23 (шаблоны операций) — ГОТОВО (см. отдельную запись выше).
- №16 (фильтры операций по категориям) — ГОТОВО (см. отдельную запись выше).
- №24 (поиск по операциям в Финансах) — ГОТОВО (см. отдельную запись выше).
- №17 (импорт/экспорт) — решили делать ТОЛЬКО экспорт в CSV, импорт не нужен.
  ГОТОВО (см. отдельную запись выше).
- №20 (календарь устройства) — решили делать ТОЛЬКО экспорт .ics («добавить
  в календарь» на событии/задаче), не полную OAuth-синхронизацию с Google.
  ГОТОВО (см. отдельную запись выше).
- №14 (повторяющиеся события) — ГОТОВО (см. отдельную запись выше). Только
  для событий календаря, не для задач-напоминаний (см. пояснение выше).

ГОТОВО (12 из 12 из первого списка, все запушены в main):
- №3 (двойные запросы) — DONE, коммит 7861ae9. Кнопки «Добавить счёт» /
  «Записать расход» / «Создать кабинет» теперь блокируются на время запроса
  (submitting state), текст меняется на «Сохраняем…».
- №6 (нельзя удалить кабинет) — DONE, коммит 7861ae9. Две причины: (а)
  confirm() не всплывает в установленном PWA — заменено на подтверждение
  в 2 тапа прямо в интерфейсе; (б) getSpaces делала SELECT-затем-INSERT без
  атомарности — гонка плодила дубли «Личный»/твой кейс с 3x «KitchenPlus».
  Исправлено на INSERT...ON CONFLICT с partial unique index
  (finance_spaces_owner_personal_idx), проверено тестом на 10 параллельных
  вызовах. Два пустых дубля KitchenPlus остались в базе — не трогал их (не
  мои данные), теперь можешь удалить сам через починенную кнопку.
- №9 (копейки не видны) — DONE, коммит 7861ae9. formatMoney (lib/financeCalc.ts)
  всегда округляла до целого; теперь показывает 2 знака автоматически, если
  они есть. Деньги в БД всегда хранились верно (numeric), это был баг экрана.
- №11 (календарь показывает вчера) — DONE, коммит 7861ae9. Dashboard.tsx
  строил «сегодня» через toISOString() (переводит в UTC) — в часовых поясах
  впереди UTC весь день показывало вчерашнюю дату. Заменено на локальную дату.
- №8 (цвета счетов) — DONE, коммит 26a5675. Убран выбор цвета из формы счёта,
  везде единый янтарный акцент (как привычки в трекере).
- №2 (логотип) — DONE, коммит 26a5675. Добавлен icon-192.png на /login,
  /register, в шапку сайдбара напоминаний и на «Главную».
- №7 (курсы валют едины на все кабинеты) — DONE, коммит 26a5675. Раньше
  жили в finance_space_settings (свои у каждого кабинета) — перенесены
  в новую таблицу finance_global_rates (одна строка на аккаунт). Сеяно из
  самой полной старой настройки. lib/finance.ts getSettings/saveSettings
  больше не принимают spaceId; API /api/finance/settings тоже.
- №12 (автообновление курсов) — DONE, коммит 26a5675. Кнопка «🔄 Обновить
  курсы автоматически» в шторке курсов дёргает /api/finance/rates/fetch
  (новый роут) → open.er-api.com (бесплатно, без ключа) → инвертирует курс
  в формат приложения. Остаётся редактируемым перед сохранением.
- №10 (редактирование операций + кто добавил) — DONE, коммит d8611bb.
  editTxn (lib/finance.ts) правит сумму/категорию/комментарий/дату у
  расхода/дохода, атомарно донастраивая баланс счёта на разницу. Переводы
  по-прежнему только удаляются (трогают 2 счёта). PATCH /api/finance/txns/[id].
  Тап по строке операции (не переводу) в истории открывает шторку
  редактирования. У каждой операции теперь виден автор (getTxns джойнит
  todo_users.username) — полезно в общих кабинетах.

- №5 (валидация форм — «писать чего не хватает») — DONE, коммит 161c165.
  Кнопки в формах (операция, счёт, категория, кабинет) больше не тупо
  задизейблены — при попытке сохранить с пустыми полями появляется
  конкретное сообщение (например «Введите сумму больше нуля», «Выбери
  счёт», «Введите название кабинета»).
- №1 + №4 (кредиты/долги/рассрочки) — DONE, коммит 161c165. Новый раздел
  внутри Финансов (⚙ → «Кредиты и долги»): таблицы finance_credits +
  finance_credit_payments (миграция scripts/migrate-credits.mjs). Три
  типа (кредит/долг/рассрочка), для долга — направление «я должен» /
  «мне должны». Заполнение (сумма, ставка, ежемесячный платёж, дата
  следующего платежа), список активных/закрытых, детали со прогресс-баром,
  оплата (частичная/полная/ежемесячный платёж одной кнопкой с выбором
  счёта списания — платёж атомарно двигает баланс счёта), история
  платежей с отменой, редактирование, удаление. При оплате точной суммы
  ежемесячного платежа дата следующего платежа сама сдвигается на месяц.
  ВАЖНО (найдено и исправлено при тестировании): в CREDIT_FIELDS
  (lib/finance.ts) столбцы были неквалифицированы алиасом — джойн с
  finance_space_members (тоже имеет space_id/user_id) давал ошибку БД
  «column reference is ambiguous»; и next_payment_date в SELECT без
  to_char() приходил не строкой, из-за чего addMonths() падал. Оба места
  исправлены, прогнан полный сценарий через API (curl): создание →
  список → платёж → отмена платежа → редактирование → удаление —
  всё сходится (остаток/баланс/архивация).

№13 (экран «Сегодня» = трекер + напоминания, стартовый экран) — DONE,
коммит после 161c165. 4-я вкладка слева в BottomNav («Сегодня», href
/today), корень / редиректит туда вместо /tracker. Показывает: привычки
на сегодня (тап = инкремент, как в трекере), просроченные задачи,
задачи на сегодня (кнопка «✓» = отметить Done), события на сегодня и
ближайшие 6 дней (задачи+события), пустое состояние если ничего нет.
Голосовой ассистент (components/VoiceAssistant.tsx) по просьбе убран
с остальных трёх вкладок — раньше висел глобально в app/(main)/layout.tsx,
теперь рендерится только внутри app/(main)/today/page.tsx. Ассистент
заодно переделан из «сказал → сразу отправилось» в чат: открывается
шторка с историей сообщений, текст (голосом или руками) сначала
попадает в поле ввода — можно поправить, потом отправить кнопкой или
Enter; ответ (успех/не понял/ошибка) приходит отдельным сообщением в
чате, а не одним экраном с результатом.

ДОРАБОТКИ сверх исходного списка (после №13, коммит f3f89b0) — родились из
обсуждения «чего не хватает на Сегодня» и разбора реального скриншота с
непересчитанной валютой:
- Кнопка «скрыть деньги» (👁/🙈) — маскирует суммы везде в Финансах и на
  Сегодня (lib/hideMoney.ts, модульный флаг + localStorage, не React-контекст).
- «Свободно на месяц» на Сегодня — раньше показывало остаток на «свободных»
  счетах (что не имело смысла — все деньги казались «свободными»). Теперь это
  ручной бюджет, который пользователь сам задаёт на месяц и который тает по
  мере трат (finance_space_settings.free_budget, миграция
  scripts/migrate-free-budget.mjs, API app/api/finance/free-budget).
- Переключатель валюты отображения итогов (₸/€/$) в Финансах. НАШЁЛ И
  ПОЧИНИЛ БАГ при тестировании: первая версия при переключении меняла
  настоящую baseCurrency кабинета (сохраняла в БД) — курсы остаются
  привязаны к старой базе, конвертация ломается («нет курса для ₸»), а заодно
  портятся бюджеты (хранятся в базовой валюте). Исправлено: переключатель
  теперь чисто display — считает кросс-курсы на лету (lib/financeCalc.ts:
  rebase()), реальная baseCurrency не трогается. «Вам должны»/«Вы должны» и
  остаток по каждому счёту показывают конвертацию в выбранную валюту кошелька.
- Карточка «Платежи в этом месяце» по кредитам/рассрочкам (сумма к оплате в
  этом месяце, список названий) — либо «Все платежи в этом месяце закрыты 🎉»,
  если нечего платить.
- В форме СОЗДАНИЯ кредита/рассрочки поле «Задолженность на сегодня» теперь
  видно сразу (раньше только при редактировании).
- Категории операций можно переименовать/сменить эмодзи прямо в шторке
  «Категории» (тап по строке → инлайн-редактор), не удаляя и создавая заново
  (PATCH /api/finance/categories/[id], lib/finance.ts updateCategory).
Всё проверено через Playwright на тестовом мультивалютном аккаунте, тестовые
данные удалены.

ВАЖНО: в этом же репозитории параллельно велась ДРУГАЯ (твоя личная) работа
над голосовым ассистентом — Plans.md (этот файл, дописан тобой), lib/assistant.ts,
app/api/assistant/route.ts, components/VoiceAssistant.tsx, app/api/finance/rates/fetch
пересекается по названию но НЕ конфликтует, package.json (добавлен
@anthropic-ai/sdk), app/(main)/layout.tsx (подключён VoiceAssistant). Ассистент
я не трогал и не коммитил — это твои файлы, при коммите я стейджил только свои.
Модель хранения: сервер держит ключ Anthropic сам, лимит 15 бесплатных голосовых
команд в месяц на пользователя (freemium) — договорились об этом в чате.

=== ИСХОДНЫЙ СПИСОК ===
1) Сделать в разделе финансы кредиты долги и рассрочки, чтобы их заполнять и потом была возможность оплатить ежемесячный или полный платеж и выбрать откуда с какого счета.
2) Добавить больше мест где бы показывался логотип. 
3) При нажатии не добавляется счет при нажатии еще раз опять отправялется запрос, нужно сделать уведомление о том что не получилось или что надо подождить или что нет интернета, много раз понажимал много раз один и тот же счет открылся... 
4) Добавить возможность редактировать уже созданные кредиты, долги и рассрочки.
5) Писать чего не хватате и что надо дозаполнить
6) Невозможно удлаить кабинет в финансах, создалось изза бага сразу 3 и не могу удалить 
7) курсы валют для всех кабинетов должны быть едины 
8) Цвета счетов нужно убрать это бессмылсенно
9) не учитывается число после запятой в добавлении чисел на кошельки
10) добавить отдельно полную историю чтобы можно было отменить или отредактировать затрату или прибыль, чтобы отследить добавлял ты это или нет.
11) не знаю почему активный день показывает календарь 10 июля, а сейчас уже 11
12) Можно ли сделать автоматичское считывание курсов валют откуда то? плагин какой-то подключить или API, чтобы не вручную каждый раз обновлять курсы валют.
13) Сделать новый экран сегодняшнего дня который будет соединением трекера и напоминаний, где будут ближайшие осбытия, то что ждет сегодня чтобы сразу видеть собрание со всего приложения там, При старте приложения открывается эта страница со всем на ней необходимым
14) Сделать возможность добавлять напоминания на определенные даты и время, с возможностью выбора повторения (ежедневно, еженедельно, ежемесячно).
15) Добавить возможность синхронизации данных между устройствами, чтобы изменения, внесенные на одном устройстве, автоматически отображались на других. МОжет это уже есть. 
16) Улучшить интерфейс для удобного просмотра и управления финансами, включая фильтры и сортировку по категориям.
17) Добавить возможность экспорта и импорта данных в формате CSV или Excel для удобного анализа и резервного копирования.
18) Реализовать систему уведомлений о предстоящих платежах, задолженностях и важных событиях в финансовом разделе.То есть сделать так чтобы писалось сколько после затрат будет денег в конце месяца с учетом лимитов на категориях и кредитов рассрочек7 
19) Голосовой ввод задач и напоминаний, чтобы пользователи могли быстро добавлять события без необходимости печатать.
20) Добавить возможность интеграции с календарем устройства для синхронизации событий и напоминаний.
21) Реализовать систему отчетов и аналитики, чтобы пользователи могли видеть свои финансовые показатели за определенные периоды времени.
22) Добавить возможность настройки уведомлений о превышении бюджета по категориям расходов.
23) Сделать возможность создания шаблонов для повторяющихся расходов и доходов, чтобы ускорить процесс добавления новых записей.
24) Улучшить систему поиска по финансам, чтобы пользователи могли быстро находить нужные записи по ключевым словам или фильтрам.
25) Возможно ли сделать как будто исскуственный интелект где ты бы открыл всего одно окно, надиктовал голосом и оно сделало функцию свою? например скзаал - трата с каспи счета 1000 тенге на продукты и закрыл приложение. или на 20 июля в 10 00 втсреча в центре 
26) 